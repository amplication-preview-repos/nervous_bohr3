/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Insight } from "./Insight";
import { InsightCountArgs } from "./InsightCountArgs";
import { InsightFindManyArgs } from "./InsightFindManyArgs";
import { InsightFindUniqueArgs } from "./InsightFindUniqueArgs";
import { DeleteInsightArgs } from "./DeleteInsightArgs";
import { InsightService } from "../insight.service";
@graphql.Resolver(() => Insight)
export class InsightResolverBase {
  constructor(protected readonly service: InsightService) {}

  async _insightsMeta(
    @graphql.Args() args: InsightCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Insight])
  async insights(
    @graphql.Args() args: InsightFindManyArgs
  ): Promise<Insight[]> {
    return this.service.insights(args);
  }

  @graphql.Query(() => Insight, { nullable: true })
  async insight(
    @graphql.Args() args: InsightFindUniqueArgs
  ): Promise<Insight | null> {
    const result = await this.service.insight(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Insight)
  async deleteInsight(
    @graphql.Args() args: DeleteInsightArgs
  ): Promise<Insight | null> {
    try {
      return await this.service.deleteInsight(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
