/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Shard } from "./Shard";
import { ShardCountArgs } from "./ShardCountArgs";
import { ShardFindManyArgs } from "./ShardFindManyArgs";
import { ShardFindUniqueArgs } from "./ShardFindUniqueArgs";
import { DeleteShardArgs } from "./DeleteShardArgs";
import { ShardService } from "../shard.service";
@graphql.Resolver(() => Shard)
export class ShardResolverBase {
  constructor(protected readonly service: ShardService) {}

  async _shardsMeta(
    @graphql.Args() args: ShardCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Shard])
  async shards(@graphql.Args() args: ShardFindManyArgs): Promise<Shard[]> {
    return this.service.shards(args);
  }

  @graphql.Query(() => Shard, { nullable: true })
  async shard(
    @graphql.Args() args: ShardFindUniqueArgs
  ): Promise<Shard | null> {
    const result = await this.service.shard(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Shard)
  async deleteShard(
    @graphql.Args() args: DeleteShardArgs
  ): Promise<Shard | null> {
    try {
      return await this.service.deleteShard(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
