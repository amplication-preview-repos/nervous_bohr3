/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Uncle } from "./Uncle";
import { UncleCountArgs } from "./UncleCountArgs";
import { UncleFindManyArgs } from "./UncleFindManyArgs";
import { UncleFindUniqueArgs } from "./UncleFindUniqueArgs";
import { DeleteUncleArgs } from "./DeleteUncleArgs";
import { UncleService } from "../uncle.service";
@graphql.Resolver(() => Uncle)
export class UncleResolverBase {
  constructor(protected readonly service: UncleService) {}

  async _unclesMeta(
    @graphql.Args() args: UncleCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Uncle])
  async uncles(@graphql.Args() args: UncleFindManyArgs): Promise<Uncle[]> {
    return this.service.uncles(args);
  }

  @graphql.Query(() => Uncle, { nullable: true })
  async uncle(
    @graphql.Args() args: UncleFindUniqueArgs
  ): Promise<Uncle | null> {
    const result = await this.service.uncle(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Uncle)
  async deleteUncle(
    @graphql.Args() args: DeleteUncleArgs
  ): Promise<Uncle | null> {
    try {
      return await this.service.deleteUncle(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
